mcp.promptbanken.se {
    handle /healthz {
        reverse_proxy 127.0.0.1:8000
    }

    handle /sse {
        reverse_proxy 127.0.0.1:8000
    }

    handle /messages/* {
        request_body {
            max_size 64KB
        }
        reverse_proxy 127.0.0.1:8000
    }

    handle /mcp {
        @options {
            method OPTIONS
        }
        handle @options {
            header Access-Control-Allow-Origin "*"
            header Access-Control-Allow-Methods "POST, OPTIONS"
            header Access-Control-Allow-Headers "Content-Type, Authorization, X-MCP-Key"
            respond 204
        }
        header Access-Control-Allow-Origin "*"
        request_body {
            max_size 64KB
        }
        reverse_proxy 127.0.0.1:8000
    }

    handle /mcp/key {
        @key_options {
            method OPTIONS
        }
        handle @key_options {
            header Access-Control-Allow-Origin "*"
            header Access-Control-Allow-Methods "POST, OPTIONS"
            header Access-Control-Allow-Headers "Content-Type, Authorization, X-MCP-Key"
            respond 204
        }
        header Access-Control-Allow-Origin "*"
        request_body {
            max_size 64KB
        }
        reverse_proxy 127.0.0.1:8000
    }

    handle /admin {
        request_body {
            max_size 64KB
        }
        reverse_proxy 127.0.0.1:8000
    }

    handle /api/v1/* {
        reverse_proxy 127.0.0.1:8000
    }

    handle /openapi.json {
        reverse_proxy 127.0.0.1:8000
    }

    handle / {
        reverse_proxy 127.0.0.1:8000
    }

    handle {
        respond "Not found" 404
    }

    log {
        output file /var/log/caddy/mcp_access.log
        format filter {
            wrap json
            fields {
                request>headers>Authorization delete
                request>headers>X-Mcp-Key delete
            }
        }
    }
}

# kommun.promptbanken.se is being retired in favor of app.promptbanken.se
# (GitHub Pages). DNS for kommun now points here (A record) instead of
# GitHub Pages so this permanent redirect can cover every path/legacy link.
kommun.promptbanken.se {
    redir https://app.promptbanken.se{uri} permanent
}
