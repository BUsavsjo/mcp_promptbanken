services:
  promptbanken-mcp:
    build:
      context: ./mcp-server
    environment:
      MCP_HOST: 0.0.0.0
      MCP_PORT: 8000
      MCP_LOG_LEVEL: INFO
      PROMPTBANKEN_MCP_API_KEY: ${PROMPTBANKEN_MCP_API_KEY:-}
    ports:
      - "8000:8000"
    restart: unless-stopped
